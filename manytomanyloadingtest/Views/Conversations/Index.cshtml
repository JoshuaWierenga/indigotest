@model IEnumerable<manytomanyloadingtest.Models.Conversation>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.GroupName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.IsGroupChat)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GroupName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsGroupChat)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ConversationId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ConversationId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ConversationId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
